<Window x:Class="BackupMaker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BackupMaker"
        mc:Ignorable="d"
        Title="Backup Maker" Height="550" Width="800"
        ResizeMode="CanMinimize">
    
    <Window.Resources>
        <local:AvailableDrives x:Key="DrivesList"/>        
    </Window.Resources>
    
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button
                Click="OpenSettingsWindows"
                Content="Excluded files"/>
        </ToolBar>
        <StackPanel HorizontalAlignment="Center">
            <TextBlock FontSize="50" HorizontalAlignment="Center">BackupMaker</TextBlock>
            <TextBlock FontSize="30" HorizontalAlignment="Center">By: Jesus Lozano</TextBlock>
            <StackPanel
                Margin="0 20 0 0"
                Orientation="Horizontal"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
                <Label>Origin disk: </Label>
                <ComboBox
                    Margin="0 0 15 0"
                    Name="Origin"
                    ItemsSource="{StaticResource DrivesList}"
                    DropDownOpened="HandleChange"
                    DropDownClosed="VerifySelection"/>
                <Label>Destiny disk: </Label>
                <ComboBox 
                    Margin="0 0 15 0"
                    Name="Destiny"
                    ItemsSource="{StaticResource DrivesList}"
                    DropDownOpened="HandleChange"
                    DropDownClosed="VerifySelection"/>
            </StackPanel>
            <TextBlock
                FontSize="15"
                HorizontalAlignment="Center"
                Visibility="Collapsed"
                Foreground="Red"
                Name="NoSoErr"
                Text="The origin drive doesn't contain a Windows operative System"/>
            <TextBlock
                FontSize="15"
                HorizontalAlignment="Center"
                Visibility="Collapsed"
                Foreground="Red"
                Name="SameDiskErr"
                Text="The origin and destiny drives can't be the same"/>
            <Button
                Width="50"
                Margin="20"
                Name="StartButton"
                IsEnabled="False"
                Click="StartBackup"
                >Start</Button>
            <TextBlock
                FontSize="15"
                HorizontalAlignment="Center"
                Visibility="Collapsed"
                Foreground="Green"
                Name="Success"
                Margin="0 0 0 10"
                Text="Backup completed"/>
            <StackPanel Name="ProgressIndicator" Visibility="Collapsed" Margin="0 0 0 10">
                <ProgressBar Width="300" Height="10" IsIndeterminate="True"></ProgressBar>
                <TextBlock HorizontalAlignment="Center">Copying, please wait.</TextBlock>
            </StackPanel>
        </StackPanel>
    </DockPanel>
</Window>
